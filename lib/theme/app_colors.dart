import 'package:flutter/material.dart';

class AppColors {
  // Neutrals
  static const Color backgroundLight = Color(0xFFF9FAFB); // Very soft gray/white
  static const Color backgroundDark = Color(0xFF1C1C1E); // Dark charcoal
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF2C2C2E);

  // Text
  static const Color textPrimaryLight = Color(0xFF1F2937);
  static const Color textSecondaryLight = Color(0xFF6B7280);
  static const Color textPrimaryDark = Color(0xFFF3F4F6);
  static const Color textSecondaryDark = Color(0xFF9CA3AF);

  // Accents (Soft/Pastel)
  static const Color primary = Color(0xFF818CF8); // Soft Indigo
  static const Color primaryDark = Color(0xFF6366F1);
  
  static const Color success = Color(0xFF34D399); // Soft Green
  static const Color error = Color(0xFFF87171); // Soft Red
  
  // UI Elements
  static const Color borderLight = Color(0xFFE5E7EB);
  static const Color borderDark = Color(0xFF374151);
}
